import React from 'react';

const Input = () => (
	<div className="input-group">
		<input type="text" placeholder="Новая зада..." />
		<button type="submit">Добавить задачу</button>
	</div>
);

export default Input;
